{
  "name": "occlusion-visualization",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "yarn clean && mkdir dist && cp index.html dist/index.html && NODE_ENV=production webpack --config webpack.config.js",
    "dev": "webpack-dev-server --watch --config webpack.config.js"
  },
  "dependencies": {
    "@mapbox/vector-tile": "^1.3.1",
    "dotenv-webpack": "^1.7.0",
    "earcut": "^2.1.5",
    "pbf": "^3.2.0",
    "regl": "^1.3.11"
  },
  "devDependencies": {
    "rimraf": "^2.6.3",
    "webpack": "^4.33.0",
    "webpack-cli": "^3.3.3",
    "webpack-dev-server": "^3.7.1"
  }
}
